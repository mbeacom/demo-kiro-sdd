# Database Configuration
# PostgreSQL connection string for Prisma
# Format: postgresql://username:password@localhost:5432/database_name
DATABASE_URL="postgresql://postgres:password@localhost:5432/animal_shelter_dev"

# NextAuth Configuration
# Secret key for JWT token signing and session encryption
# Generate a random secret: openssl rand -base64 32
NEXTAUTH_SECRET="your-super-secret-jwt-key-change-this-in-production"

# Base URL for NextAuth callbacks and redirects
# In development: http://localhost:3000
# In production: https://yourdomain.com
NEXTAUTH_URL="http://localhost:3000"

# Optional: Node Environment (automatically set by most deployment platforms)
NODE_ENV="development"
